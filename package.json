{
  "name": "blog",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn workspaces run develop",
    "lint": "yarn workspaces run lint",
    "build": "yarn workspaces run build",
    "build:ci": "yarn workspaces run build:ci"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
